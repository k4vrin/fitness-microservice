spring:
  application:
    name: activityservice
  data:
    mongodb:
      uri: ${ACTIVITY_DB_URI}
      database: fitness_activity_db
      username: ${ACTIVITY_DB_USERNAME:activityservice}
      password: ${ACTIVITY_DB_PASSWORD:activityservice}



server:
  port: ${ACTIVITY_SERVICE_PORT:8082}